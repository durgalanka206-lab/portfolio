# Gmail address to send verification emails from (e.g. your_email@gmail.com)
EMAIL_USER=your_email@gmail.com

# Gmail App Password (NOT your regular password. Generate it in Google Account security settings)
EMAIL_PASS=your_gmail_app_password

# Port for backend server to listen on (defaults to 5000)
PORT=5000
